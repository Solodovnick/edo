жа# Скопируйте в `.env` в корне `edo/` (файл не коммитится).
# PostgreSQL из docker-compose: пользователь edo, пароль edo, БД edo, порт 5432

DATABASE_URL=postgresql://edo:edo@127.0.0.1:5432/edo

# Порт Node API (проксируется из Vite как /api → 3001)
API_PORT=3001
